kind: ConfigMap
apiVersion: v1
metadata:
  name: "{{ .Chart.Name }}-customer-config-{{ .Chart.Version }}"
  namespace: "{{ .Values.global.namespace }}"
  labels:
    app: "{{ .Chart.Name }}"
    version: "{{ .Chart.Version }}"
data:
  values.yaml: |-
{{ .Files.Get "static/values-custom-config.yaml" | indent 4}}
