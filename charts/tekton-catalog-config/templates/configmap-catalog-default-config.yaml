kind: ConfigMap
apiVersion: v1
metadata:
  name: "{{ .Chart.Name }}-custom-default-{{ .Chart.Version }}"
  namespace: "{{ .Values.global.namespace }}"
  labels:
    app: "{{ .Chart.Name }}"
    version: "{{ .Chart.Version }}"
data:
  values.yaml: |-
{{ .Files.Get "static/values-custom-default.yaml" | indent 4}}
